import 'package:flutter/material.dart';

class Shedule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: const Center(child:Text('Shedule')),
            backgroundColor: const Color(0xff6C757D),
        ),
        body: Center(
            child: Container()
        ),
    );
  }
}
